// Custom styles.
.post-block {
   margin-top: 60px;
   margin-bottom: 60px;
   padding: 25px;
   box-shadow: 0 0 20px rgba(202, 203, 203, .5);
}

div.sidebar-toggle {
   bottom: 80px;
}

div.back-to-top.back-to-top-on {
   bottom: 54px;
}

// 修改成你期望的宽度
$content-desktop = 80%

// 当视窗超过 1600px 后的宽度
$content-desktop-large = 80%